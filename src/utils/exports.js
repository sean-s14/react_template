import convertDate from './convertDate';
import { isObject } from './dataType';
import { isMoreThanOneCookie, getCookieInfo, deleteCookie } from './cookies';

export {
    convertDate,
    isObject,
    isMoreThanOneCookie, getCookieInfo, deleteCookie
}